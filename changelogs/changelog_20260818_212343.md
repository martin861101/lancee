# OpenConnector catalog and responsive navigation — 2026-08-18

- Expanded the Connections page from the first 100 OpenConnector providers to
  the complete live catalog, capped at 2,000 entries.
- Added dynamic provider categories with an `Other` fallback, full-catalog
  search, provider counts, and incremental batches of 60 cards.
- Replaced provider initials with upstream official provider icons when
  available, retaining initials only as an image fallback.
- Preserved provider descriptions, homepages, advertised auth types, and public
  OAuth authorization/token URLs in the Lancee provider contract.
- Kept OAuth connect/disconnect workspace-scoped and left API-key/custom
  credentials in OpenConnector for later provider configuration.
- Enabled OpenConnector by default in Docker Compose and the example
  environment while retaining `OPENCONNECTOR_ENABLED=false` as the opt-out.
- Restored the public landing navbar below 1,100px with an accessible hamburger
  menu containing every public navigation destination.
- Updated the root README and OpenConnector integration documentation.
- Verified the OpenConnector adapter/gateway flow; production build and lint
  were also run, with only pre-existing storefront/template lint warnings.
