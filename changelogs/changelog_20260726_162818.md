# Changelog — 2026-07-26 16:28:18 UTC

## Administrator access verification

- Confirmed that the configured administrator email matches the intended lancee
  owner account.
- Confirmed that the supplied password matches the stored 64-byte scrypt hash;
  no plaintext password was written to disk.
- Reloaded the `nexus-agents-platform` PM2 process with the existing secure
  environment.
- Verified the production HTTPS authentication flow: login `200`, authenticated
  session `200`, and logout `204`.
- Added a concise administrator sign-in and password-rotation runbook to
  `AUTH_AND_NOTIFICATIONS.md` and linked the live access point from `README.md`.

