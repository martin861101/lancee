# Google OAuth callback compatibility

## Fixed

- Added `/api/integrations/google/callback` as a supported Google Drive OAuth
  callback so the production URI already registered in Google Cloud no longer
  returns HTTP 404.
- Added an integration-flow regression check for the configured callback alias.

## Documentation

- Documented the canonical callback and both supported compatibility aliases in
  the Google OAuth guide, integrations guide, and README.
