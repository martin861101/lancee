# Changelog — 2026-07-26 17:34 UTC

## Improvement-plan review

- Reviewed the improvement plan against the current documentation, source, and
  implementation history.
- Kept the depth-before-breadth direction while moving durable domain records
  ahead of payment, n8n, and offline work.
- Corrected the Paystack proposal to use its documented server-side secret-key
  authentication for the current single-workspace deployment instead of an
  assumed OAuth flow.
- Deferred offline mutation queuing until durable server records and stable
  mutation identifiers provide an authoritative synchronization target.

## Automation terminology

- Renamed the internal `Agent` model to `Automation`.
- Renamed run references to `automationId` and `automationName`.
- Renamed `mockApi.agents` to `mockApi.automations`.
- Updated React component, state, prop, handler, and CSS names to use automation
  terminology.
- Updated the documented production replacement routes to
  `/v1/automations`.
- Corrected README links so the referenced documents resolve under `docs/`.
- Preserved the existing public hostname and legacy PM2 process name.

## Verification

- `pnpm build`
- `pnpm lint`
