# Changelog — 2026-08-06 08:39 UTC

## Fixed

- Routed explicit natural-language file-writing requests to the Lancee
  `create_file` tool instead of asking the AI provider to select it from the
  entire MCP catalog.
- Kept file writes behind the existing risk-labelled confirmation step.
- Automatically scrolled the assistant conversation to newly returned approval
  cards and action status updates.

## Verification

- Extended `scripts/verify-codex-connector.mjs` to cover the complete path from
  a file-writing prompt through the proposed action, approval invocation, and
  persisted Files record.
- `npm run build`
- `npm run lint` (passes with pre-existing storefront warnings)
- `npm run verify:ai`
- `npm run verify:codex-connector`
