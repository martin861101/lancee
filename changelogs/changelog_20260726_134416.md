# Changelog — 2026-07-26 13:44:16 UTC

## n8n integration

- Added a dedicated n8n integration card and configuration modal.
- Added a manually entered n8n DNS webhook with `https://n8n.hygridtech.co.za/webhook/nexus` as the UI example.
- Added the generated Nexus callback at `https://agents.hygridtech.co.za/api/hooks/n8n/acme-demo`.
- Added separate GET and POST tests for Nexus → n8n and n8n → Nexus.
- Added shared-signature configuration and explicit `X-Nexus-Signature` guidance.
- Added typed mock configure, trigger, and disconnect actions.

## MCP Services integration

- Modeled the architecture documented in `/home/apps/mcp` and `mcp-conf`.
- Added DNS gateway discovery through `https://mcp.hygridtech.co.za`.
- Added server-side workspace-token status without exposing credentials in the UI.
- Added four runtime service cards representing 20 documented MCP tools.
- Added service activation/deactivation, capability synchronization, and representative tool invocation.
- Verified activation and execution of `browser-worker` / `website_smoke_test`.
- Documented the stable `/api/v1/capabilities` and `/api/v1/tools/{tool_id}/call` routes.

## Hosting

- Changed Vite development and preview ports to `5177`.
- Added `agents.hygridtech.co.za` as an allowed host.
- Added a PM2 production process in `ecosystem.config.cjs`.
- Built and started `nexus-agents-platform`, then saved the PM2 process list.
- Verified Nginx Proxy Manager forwards `agents.hygridtech.co.za` to port `5177`.
- Verified the public HTTPS endpoint returns HTTP 200 with a valid certificate.

## Validation

- TypeScript production build passes.
- Current application source lint passes.
- All four n8n request paths were exercised.
- MCP connect, discovery, activation, and tool-test flows pass without browser console errors.
