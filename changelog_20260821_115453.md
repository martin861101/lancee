# Workspace Pulse Home — 2026-08-21 11:54:53 UTC

## Changed

- Rebuilt Home around a large weather-responsive Workspace Pulse using
  `public/img/sunny.png`.
- Added a concise Today action list sourced from real projects, tasks,
  deadlines, invoices, automation activity, and notifications.
- Preserved Quick Task, saved automations, recent activity, current navigation,
  and the existing Analytics implementation.
- Added responsive sunny, cloudy, rainy, storm, snow, night, and no-weather
  presentation states with reduced-motion support.

## Backend

- Added authenticated `GET /api/workspace/pulse`.
- Added deterministic fallback copy and actionable Today items.
- Added per-workspace/per-user AI pulse caching with a 90-minute TTL.
- Reused `completeChat` for bounded copy generation; no agent runtime or direct
  Hermes agent call was introduced.
- Added strict JSON validation, plain-text sanitization, length limits, and
  silent fallback behavior.

## Verification

- `npm run build`
- `npm run lint` (passes with existing unrelated warnings)
- `npm run verify:workspace-pulse`
- `npm run verify:ui-fixes`
- Production-build browser check at 1440×1000 and 390×844 with no horizontal
  overflow or page errors.
