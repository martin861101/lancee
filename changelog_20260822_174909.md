# Add Mail Connected Intelligence — 2026-08-22 17:49:09 UTC

## Changed

- Instrumented the existing IMAP Inbox sync and accepted SMTP send points with
  idempotent `communication.received` and `communication.sent` workspace
  events without changing Mail transport or duplicating bodies.
- Added workspace-scoped canonical People, provider-neutral communication
  metadata observations, and confirmed thread/project relationships.
- Unified Mail participant and Calendar attendee email identity through exact,
  case-normalized Person resolution and conservative Client relationships.
- Added deterministic project, client, and Person communication features plus
  the cross-source `client_attention_load` detector backed by communication and
  completed-meeting evidence.
- Added authenticated communication feature, attention detector, and Mail
  project-link APIs plus a small relationship control in the existing reader.
- Documented provider neutrality, identity and relationship rules, feature and
  detector calculations, privacy controls, Phase 1 coverage limits, semantic
  extension boundaries, and the Phase 3 value/attention recommendation.

## Schema

- Added `connected_people`.
- Added metadata-only `communication_messages`.
- Added confirmed `communication_thread_links`.
- Reused `workspace_events` and `connected_opportunities` unchanged.

## Verification

- `npm run verify:connected-intelligence`
- `npm run verify:signals`
- `npm run verify:dynamics`
- `npm run verify:decision-phase2`
- `npm run verify:decision-phase3`
- `npm run verify:mcp`
- `npm run verify:mail`
- `npm run verify:mail-automation`
- `npm run build`
- `npm run lint` (passes with existing repository warnings)
- `git diff --check`
