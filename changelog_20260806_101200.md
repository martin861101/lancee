# Docker Baileys install fix

The Docker build now installs locked dependencies with pnpm's
`--ignore-scripts` flag. This prevents pnpm 11 from failing on unapproved
Baileys/protobufjs install scripts while keeping third-party install scripts
disabled inside the image. The frontend build remains the verification step
after dependency installation.
