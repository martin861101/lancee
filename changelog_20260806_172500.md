# MCP Grid integration

- Applied integration plan `3527e5a9-20b6-498f-913c-3b0ba403e15e` from
  `https://mcp.hygridtech.co.za`.
- Generated the managed client module at `src/integrations/mcp-grid.ts` with
  29 selected tools and the plan's skill metadata.
- Added the in-repository `mcp-grid-client` compatibility package and updated
  `pnpm-lock.yaml` so container builds do not depend on a sibling checkout.
- Copied the vendored package into Docker's dependency layer before the frozen
  install step.
- Kept `MCP_API_TOKEN` server-side; no credential was written to generated
  source or documentation.
