# Public MCP gateway endpoint

- Documented `https://lancee.hookitupservices.com/mcp` as the public MCP
  endpoint, forwarded to the app on port `5177`.
- Confirmed the repository route is implemented; the currently deployed image
  must be rebuilt and restarted before the live endpoint returns MCP JSON-RPC
  responses.
