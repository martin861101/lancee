# Add Connected Intelligence synthetic benchmark — 2026-08-22 19:15:43 UTC

## Added

- Deterministic `business-records.v1.json` normalized from the permanent XLSX
  source: 1,517 source records across 11 object types.
- Marker-protected `Connected Intelligence Test` workspace creation, existing
  owner membership, dry-run, duplicate rejection, exact reset, and reseed.
- Workspace-scoped fixture refs and narrow quote, time, payment, project
  approval, and project change representations, plus missing historical fields
  on projects, tasks, and invoices.
- Marker-authorized fixture communication ingestion through the canonical
  Person/message/workspace-event path without a Mail account.
- Historical client/project/task/quote/invoice/payment/meeting/communication
  events, shared Mail/Calendar Person identity, real feature calculation, and
  real detector execution.
- Ground-truth comparator with explicit positives/negatives, evidence and
  isolation checks, false-positive/false-negative reporting, and side-effect
  firewall verification.
- `fixture:ci`, `seed:ci`, `benchmark:ci`, and `verify:ci-fixture` commands.

## Safety

- Reset requires exact fixture purpose, dataset, version, source SHA-256, owner,
  and owner membership; it deletes only the resolved fixture workspace ID.
- No display-name deletion, shared-table truncation, fake authentication, Mail
  account, SMTP/IMAP, external Calendar, payment provider, webhook,
  notification, automation, integration execution, Signal Engine semantic
  processing, or Hermes invocation is used.

## Benchmark

- Expected/detected opportunities: 11/11.
- True positives: 11.
- False positives: 0.
- False negatives: 0.
- Both `project_meeting_load` and `client_attention_load` pass.

## Verification

- PASS — deterministic fixture conversion and validation.
- PASS — dry-run performs no database writes.
- PASS — isolated seed, duplicate rejection, benchmark, reset, reseed, and
  second benchmark.
- PASS — normal workspace survives fixture reset.
- PASS — fixture communication is rejected outside a marked workspace.
- PASS — canonical event counts, evidence integrity, shared Person identity,
  historical timestamps, opportunity deduplication, and side-effect firewall.

- PASS — `npm run verify:ci-fixture`
- PASS — `npm run verify:connected-intelligence`
- PASS — `npm run verify:signals`
- PASS — `npm run verify:dynamics`
- PASS — `npm run verify:decision-phase2`
- PASS — `npm run verify:decision-phase3`
- PASS — `npm run verify:mcp`
- PASS — `npm run verify:mail`
- PASS — `npm run verify:paystack`
- PASS — `npm run verify:workspace-flows`
- PASS — `npm run build`
- PASS — `npm run lint` (existing warnings only)
- PASS — `git diff --check`
- ENVIRONMENT BLOCKED — `npm run verify:postgres` requires `DATABASE_URL` or
  `PGHOST`; neither is configured in this workspace.
