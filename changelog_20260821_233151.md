# Restore full Hermes native media handling — 2026-08-21 23:31:51 UTC

## Fixed

- Kept Hermes' complete profile-provided agent capabilities available in
  WorkspaceChat, including native skills, browser screenshots, terminal/code,
  files, memory, media tools, and subagents.
- Imported verified native Hermes `MEDIA:` screenshots from approved profile
  cache roots into Lancee Files and rendered them with authenticated document
  URLs instead of rejecting their filesystem paths.
- Preserved public HTTP(S) URLs whose paths contain words such as `/app`,
  `/home`, or `/workspace`; these URLs are no longer mistaken for local paths.
- Added shared-volume media-root and size-limit configuration for deployments
  where Hermes and Lancee run in separate containers.

## Verification

- `npm run verify:agent`
- `npm run lint`
- `npm run build`
