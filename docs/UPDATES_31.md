# Dashboard Updates/Changes

**0. Navbar (landing page);**

- Remove the icons next to the Connections text

1. **Alll pages on the dashboard;**

- Make the background of all pages a lighter a color than than the sidebar (make sure the colors match well)
- Make the headings every second word the same color/font as the text: Keep your freedom. on the landing page
- When clicking on the profile icon top right, create a proper profile section, and do so for settings as well. The technical settings should be called Dev Tools unde settings (database views etc.) we have to keep in mind that non tech people wikll be using this platform

**2. Project Tab:**

- Create real sections for "▦ Board
≡ Details
◫ Files
1
↗ Links" board is fine but the rest just opens the edit section for the projects
- Under project brief there seems to be 2 static buckets that are not interactable
- on each bucket the 3 dotted lines (edit) do nothing
- Add featurew to assign users to buckets/tasks
_ Add feature to add custom bucket sections

**Ideas Panel:**

- In the backend in src/components/canvasui/library there are library extensions that I added. Make that usable/imported at all times in the canvas
- Add feature to generate pdf from created canvas and sync to project files

**Files:**

- Add a remove from platform button for the files
- Change the layout to be more modern and file explorer friendly and practical
- Next to each file/folder you can add the 3 dotted icon with dropdown: Link, Edit, view, download or delete
- Relationship logic must still be there but better it

**Automations:**

- This section will be dedicated to just n8n workflows
- In the automations page add a n8n logo and showcase image so users understand what it is

**New Tab: Workflows:**

- Add a new tab called Workflows
- In the workflows tab ad a few popular day to day workflows like approvals, notifications, trigger bases flows, and a few other business process automated workflows

**Add section: Invoicing:**

- Add a new section called Invocing
- Add 3 invocing templates (styles) usert can choose from make them professional
- Add abbility to add custom fields, currencies. I will ad a currencylayer api key in the .env file for auto conversions (dont test I'll add it later)
- For the invoices add option to add pay me button on the incoice (radio button) if enabled slecetion must branch to either stripe, paypal or paystack
- optional estimates and reciepts must be available

**Activuity logs**:

- this can move to dev tools section in settings

**Connections**

- Remove the two Codex connections
- Add General AAI Connection compatible with most providers. We will configure the platform ai layer later
- Add a few more popular connections

**Team**

- Needs to have a funtion to remove or edit member, editing member needs to have roles to edit (Admin, collaborator, vie3w etc)

API keys section needs to move to dev tools





