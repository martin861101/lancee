# Changelog — 2026-07-31 22:17 UTC

## Phase 3 platform implementation

- Installed and enabled Redis 8.0.5 on the host; added `REDIS_URL` and queue
  prefix configuration, base Compose Redis, and the private Edge Redis overlay.
- Added the Core automation worker with bounded permission-checked tools,
  Redis queueing, in-process fallback, persisted execution events, run logs,
  and health reporting.
- Added project job cards, client-linked draft invoices, hashed expiring client
  approvals, attachment-safe public review pages, project comments, and
  workspace notifications surfaced in the dashboard notification popover.
- Added invoice editing, send flow, signed mock payment page, and project/job
  card completion after invoice send.
- Added the Services dashboard page, server-scoped workspace AI chat, read-only
  structured data actions, and an approval response for AI `execute` actions.
- Added PostgreSQL migration ordering, Docker `redis-tools`, Phase 3 integration
  verification, and unified navy auth/landing loading styles.

## Verification

- `npm run lint` — passed.
- `npm run build` — passed.
- `npm run verify:core-edge` — passed, including real Core side effects and the
  approval-to-payment workflow.
