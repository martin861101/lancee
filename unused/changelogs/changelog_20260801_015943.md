# Changelog — 2026-08-01 01:59 UTC

## Tokenized artwork review integration

- Installed `@annotorious/react` and its required `openseadragon` peer.
- Integrated annotation tools into the existing Projects file-to-client
  workflow.
- Added tokenized `/review/:reviewId?token=…` client links, hashed token
  persistence, protected artwork streaming, and PostgreSQL-backed review
  sessions and annotations.
- Added client comment/priority/category editing, submit validation and
  read-only freeze, plus designer filters and annotation status management.
- Removed raw project-file attachments from approval emails.
- Enabled workspace signup by default, with `ALLOW_REGISTRATION=false` as the
  opt-out.

## Verification

- `pnpm build` — passed.
- `pnpm lint` — passed.
- `node --check server/index.mjs` — passed.
- `node --check server/database.mjs` — passed.
- `pnpm verify:postgres` — not run to completion because this workspace has no
  `DATABASE_URL` or `PGHOST` configured; its script requires a live PostgreSQL
  connection.
