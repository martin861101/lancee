# Fresh PostgreSQL reset — 2026-07-28 22:26 UTC

## Completed

- Verified the reset target was the `agent-app` Docker Compose project.
- Created and validated a custom-format backup of the previous database at
  `.runtime/backups/pre-reset-20260728_222631.dump`.
- Stopped the app and database containers and removed only the confirmed
  `agent-app_pgdata` volume.
- Rebuilt the app and started a new PostgreSQL 16 instance on a newly created
  `agent-app_pgdata` volume.
- Confirmed PostgreSQL is healthy and `/api/health` returns successfully.
- Confirmed the configured bootstrap administrator and workspace exist.
- Confirmed the new database has no projects, automations, or invoices.
- Added the backup, reset, and recovery cautions to `README.md`.

## Result

The Docker deployment now uses a fresh PostgreSQL database. The prior database
can be recovered from the ignored `.runtime/backups/` dump if needed.
