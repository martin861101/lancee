# Changelog 2026-07-29 06:58:52 UTC

## Embedded Codex integration

- Added a private stdio JSON-RPC manager for `codex app-server`.
- Added isolated per-workspace/user `CODEX_HOME` directories under `.runtime`.
- Added native OpenAI `chatgptDeviceCode` login, account status, and logout
  routes.
- Added safe thread creation, text turns, turn interruption, buffered events,
  and authenticated SSE streaming.
- Added fixed workspace-root enforcement, a managed least-privilege permission
  profile, disabled tool network access, and `approvalPolicy: "never"`.
- Added a **Codex Workspace** card and full setup/task panel to Connections.
- Renamed the original outward connector card to **lancee AI for Codex** so the
  two integration directions are explicit.
- Updated the production image to install the pinned Codex CLI and added the
  `/workspace` Compose mount.
- Added `CODEX_BINARY`, `CODEX_WORKSPACE_ROOT`, and `CODEX_WORKSPACE_PATH`
  setup documentation.
- Added `pnpm verify:codex-runtime` with a deterministic in-memory App Server
  protocol fixture.

## Verification

- `pnpm build`
- `pnpm lint`
- `pnpm verify:codex-runtime`
- Real `codex-cli 0.145.0` App Server initialize/account smoke test with an
  isolated unauthenticated home
